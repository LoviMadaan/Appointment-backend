databases:
  - name: fullstack
    databaseName: fullstack_0ddt
    user: fullstack_0ddt_user

services:
  - type: web
    name: doctor
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: fullstack_0ddt
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false